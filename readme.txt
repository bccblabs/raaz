[done] remove notifications pane -> use red dots on user's page
[done] change the scoll in build.js back to tags (for simplicity)

refactor settings to be icon on the user's photo

add ability to search user's builds/parts on user's page

refactor user's page to be vehicle details

add share user's page, build's page into qr code tap (modal?)


tap on image, scroll 360 car image/model to add/view parts


